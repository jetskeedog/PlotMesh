<h1>Browse Movies</h1>
 
<% @movies.each do |movie| %>
  <div class="col-md-2">
    <div class='box panel panel-default'>
      <p><%= link_to movie.title, movie %></p>
      <%= link_to (image_tag movie.movie_poster.url(:thumb)), movie %>
    </div>
  </div>

<% end %>
