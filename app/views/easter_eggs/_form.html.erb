<%= form_for([@movie, @easter_egg]) do |f| %>
<% if @easter_egg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@easter_egg.errors.count, "error") %> prohibited this Easter Egg from being saved:</h2>
      <ul>
        <% @easter_egg.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>