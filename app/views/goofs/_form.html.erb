<%= form_for([@movie, @goof]) do |f| %>
  <% if @goof.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@goof.errors.count, "error") %> prohibited this goof from being saved:</h2>
      <ul>
      <% @goof.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>